[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma db push && node prisma/seed.mjs && npx next start -p $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
