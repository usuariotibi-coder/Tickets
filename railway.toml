[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma db push && node prisma/seed.mjs && npm run start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
